[project]
name = "personal-ai"
version = "0.1.0"
description = "A single personal agent over one git-backed knowledge repo, reachable via Telegram."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=0.0.1",
    "python-telegram-bot>=21.0",
    "duckdb>=1.0",
    "apscheduler>=3.10",
    "logfire>=0.50",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
